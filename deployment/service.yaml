apiVersion: v1
kind: Service
metadata:
  labels:
    app: library
  name: library-dep-service
  namespace: lib-nmsc
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: library
  type: LoadBalancer
